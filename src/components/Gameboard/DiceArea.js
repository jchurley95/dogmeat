import React from 'react';
import {
    DiceAreaContainer
} from './Styled/Gameboard.styled';

const DiceArea = (props) => {
    return (
        <DiceAreaContainer>
            
        </DiceAreaContainer>
    );
};

export default DiceArea;